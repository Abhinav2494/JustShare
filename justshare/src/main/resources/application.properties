spring.application.name=justshare

## =====================================================
## DATABASE
## =====================================================

spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

#spring.datasource.driver-class-name=org.postgresql.Driver

## =====================================================
## JPA / HIBERNATE
## =====================================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

## =====================================================
## FILE UPLOAD LIMIT
## =====================================================

spring.servlet.multipart.max-file-size=2GB
spring.servlet.multipart.max-request-size=2GB

## Keep uploaded files on disk instead of memory
spring.servlet.multipart.file-size-threshold=2KB
app.encryption.key=${APP_ENCRYPTION_KEY}

#spring.application.name=justshare
#
## =====================================================
## LOCAL POSTGRESQL DATABASE
## =====================================================
#
#spring.datasource.url=jdbc:postgresql://localhost:5432/justshare
#spring.datasource.username=postgres
#spring.datasource.password=wizardboy20
#
#spring.datasource.driver-class-name=org.postgresql.Driver
#
## =====================================================
## JPA / HIBERNATE
## =====================================================
#
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#
## =====================================================
## FILE UPLOAD
## =====================================================
#
#spring.servlet.multipart.max-file-size=500MB
#spring.servlet.multipart.max-request-size=500MB
#spring.servlet.multipart.file-size-threshold=2KB
#server.address=127.0.0.1
#
## =========================================================
## AES-256-GCM encryption key
## =========================================================
## Generate a key with:
##
## openssl rand -base64 32
##
## Put the generated Base64 value in your environment:
##
## APP_ENCRYPTION_KEY=<generated-value>
##
## Then use:

